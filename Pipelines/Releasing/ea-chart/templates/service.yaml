apiVersion: v1
kind: Service
metadata:
  name: {{ include "ea-chart.fullname" .}}
  labels:
    {{- include "ea-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ea-chart.selectorLabels" . | nindent 4 }}